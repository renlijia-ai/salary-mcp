{
  "name": "mcp-server-salary-web",
  "version": "0.0.7",
  "description": "MCP server for using the salary Web API",
  "license": "MIT",
  "homepage": "",
  "bugs": "",
  "type": "module",
  "bin": {
    "mcp-server-salary-web": "dist/src/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/**/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.0.1",
    "@types/node-fetch": "^2.6.12",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}
